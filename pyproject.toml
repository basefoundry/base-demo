[project]
name = "base-demo"
version = "0.0.0"
description = "Reference Base-managed project and representative demo environment."
requires-python = ">=3.13,<3.14"
dependencies = ["base-cli==0.4.2", "click", "PyYAML"]

[tool.uv]
package = false
