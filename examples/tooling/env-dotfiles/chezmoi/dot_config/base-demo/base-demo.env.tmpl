# Reference-only chezmoi-managed environment defaults for base-demo.
# Base does not read this file; it is an example for user-owned dotfiles.

export BASE_DEMO_ENV="{{ .base_demo_env | default "baseline" }}"
